export function DisclaimerBanner() {
  return (
    <aside className="border border-ember/25 bg-ember-soft/70 px-4 py-3 text-sm text-ink-soft">
      <strong className="font-semibold text-ink">Not a safety verdict.</strong> Scores, matched
      comparisons, and successful simulations are observations. Empty findings are not a pass. This
      tool does not replace a security audit.
    </aside>
  );
}
